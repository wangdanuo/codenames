{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "npm: compile",
      "type": "shell",
      "command": "source ~/.nvm/nvm.sh && nvm use 20 && npm run compile",
      "args": [],
      "cwd": "${workspaceFolder}",
      "problemMatcher": ["$tsc"],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}